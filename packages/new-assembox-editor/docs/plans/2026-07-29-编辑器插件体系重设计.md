# 编辑器插件体系重设计 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 new-assembox-editor 插件体系重构为 `definePlugin()` 工厂（对象式）+ 实例级注册 + contributes 自动应用 + dep 拓扑排序 + setup dispose 统一回收。

**Architecture:** `EditorPluginObject` 契约（contributes 静态 / setup 动态 / 生命周期钩子三入口）；`PluginManager` 实例化所有调度逻辑（activate 内聚 contributes 自动应用），`editor.start()` 收敛；废弃模块级全局注册表，根治多实例污染与资源泄漏。

**Tech Stack:** TypeScript、Vue3、vitest（单测）、playwright（e2e 回归）、pnpm。

**Spec:** `docs/07-编辑器插件体系重设计.md`

---

## 文件结构

| 文件 | 职责 | 操作 |
|---|---|---|
| `package.json` | 加 vitest devDep + test:unit 脚本 | 修改 |
| `vitest.config.ts` | vitest 配置（jsdom 环境） | 新建 |
| `src/core/plugin-types.ts` | EditorPluginObject 契约 + definePlugin + PluginContext | 重写 |
| `src/core/plugin-manager.ts` | PluginManager（实例级/dep/contributes自动/dispose） | 重写 |
| `src/core/editor.ts` | start 收敛、ctx 加 skeleton/getPlugin、createEditor 加 plugins/disableBuiltin | 修改 |
| `src/plugins/builtin-plugins.tsx` | 7 插件 definePlugin 包装 + coreRegistry 改 contributes | 修改 |
| `src/plugins/register.ts` | 移除全局自注册 | 修改/删 |
| `src/index.ts` | 导出 definePlugin/EditorPluginObject/PluginContext | 修改 |
| `src/core/__tests__/plugin-manager.test.ts` | PluginManager 单测 | 新建 |

---

## Task 1: 搭建 vitest 单测环境

**Files:**
- Modify: `package.json`
- Create: `vitest.config.ts`

- [ ] **Step 1: 安装 vitest 及 jsdom**

```bash
pnpm add -D vitest@^2 jsdom@^25
```

> 若 pnpm 因代理失败，改用 `pnpm add -D vitest@^2 jsdom@^25 --registry=https://registry.npmmirror.com`

- [ ] **Step 2: 创建 vitest.config.ts**

```ts
import {defineConfig} from 'vitest/config';

export default defineConfig({
  test: {
    environment: 'jsdom',
    include: ['src/**/*.test.ts'],
    globals: false
  }
});
```

- [ ] **Step 3: 在 package.json scripts 加入 test:unit**

在 `"scripts"` 内 `"test:e2e"` 上方加入：

```json
"test:unit": "vitest run",
"test:unit:watch": "vitest",
```

- [ ] **Step 4: 验证 vitest 可运行（无测试时退出码 0）**

Run: `pnpm test:unit`
Expected: `No test files found` 且退出码 0。

- [ ] **Step 5: Commit**

```bash
git add package.json pnpm-lock.yaml vitest.config.ts
git commit -m "test(new-assembox-editor): 引入 vitest 单测环境"
```

---

## Task 2: definePlugin 契约 + EditorPluginObject + PluginContext 类型

**Files:**
- Rewrite: `src/core/plugin-types.ts`
- Test: `src/core/__tests__/plugin-manager.test.ts`（先建空文件占位）

- [ ] **Step 1: 重写 src/core/plugin-types.ts**

```ts
/**
 * 插件契约与上下文类型定义
 * definePlugin 工厂（对象式）+ contributes(静态)/setup(动态)/钩子(事件) 三入口
 */
import type {Editor} from './editor';
import type {EditorStore} from './store';
import type {DIContainer} from './di-container';
import type {EventBus} from './event-bus';
import type {Skeleton} from '../skeleton/skeleton';
import type {ComponentRegistry} from '../registry/component-registry';
import type {SetterRegistry} from '../registry/setter-registry';
import type {AssetRegistry} from '../registry/asset-registry';
import type {ActionRegistry} from '../registry/action-registry';
import type {
  ComponentMeta,
  SetterMeta,
  ActionMeta,
  AssetMeta,
  PanelItem,
  ToolbarItem,
  ContextMenuItem
} from '../schema/types';

/** 插件可注入的上下文 */
export interface PluginContext {
  editor: Editor;
  store: EditorStore;
  di: DIContainer;
  bus: EventBus;
  skeleton: Skeleton;
  componentRegistry: ComponentRegistry;
  setterRegistry: SetterRegistry;
  assetRegistry: AssetRegistry;
  actionRegistry: ActionRegistry;
  /** 跨插件协作：按 id 查找其它插件及其 options */
  getPlugin<T = any>(id: string): {plugin: EditorPluginObject<T>; options?: T} | undefined;
}

/** 插件声明式贡献（任选，静态自动注册） */
export interface PluginContributes {
  components?: ComponentMeta[];
  setters?: SetterMeta[];
  actions?: ActionMeta[];
  assets?: AssetMeta[];
  skeleton?: SkeletonContribution[];
}

/** 骨架面板贡献 */
export interface SkeletonContribution {
  area:
    | 'topArea'
    | 'leftArea'
    | 'leftFixedArea'
    | 'leftFloatArea'
    | 'centerArea'
    | 'rightArea'
    | 'bottomArea';
  type: 'Widget' | 'Panel' | 'PanelDock';
  name: string;
  content?: any;
  contentProps?: Record<string, any>;
  panelProps?: Record<string, any>;
  props?: Record<string, any>;
  disabledPanelCache?: boolean;
}

/** 插件对象契约 */
export interface EditorPluginObject<TOptions = any> {
  readonly id: string;
  name?: string;
  priority?: number;
  scene?: string | string[];
  dep?: string[];
  contributes?: PluginContributes;
  setup?(ctx: PluginContext, options?: TOptions): void | Promise<void> | (() => void | Promise<void>);
  beforeInsert?(context: any): void | false | Promise<void | false>;
  afterInsert?(context: any): void;
  beforeUpdate?(context: any): void | false | Promise<void | false>;
  afterUpdate?(context: any): void;
  beforeDelete?(context: any): void | false | Promise<void | false>;
  afterDelete?(context: any): void;
  beforeMove?(context: any): void | false | Promise<void | false>;
  afterMove?(context: any): void;
  buildPanels?(node: any, panels: PanelItem[]): void;
  buildToolbars?(node: any, toolbars: ToolbarItem[]): void;
  buildContextMenu?(node: any, menus: ContextMenuItem[]): void;
}

/** 工厂函数：类型守卫 + 保留扩展点（默认值注入） */
export function definePlugin<TOptions = any>(
  def: EditorPluginObject<TOptions>
): EditorPluginObject<TOptions> {
  return def;
}

export type {ActionMeta};
```

- [ ] **Step 2: 暂时保留 plugin-manager.ts 不动（Task 3 重写）——先让 typecheck 不报错**

由于 EditorPluginObject 替代了旧 EditorPlugin，plugin-manager.ts/builtin-plugins.tsx/editor.ts 仍引用旧类型。本步骤仅保证 plugin-types.ts 自身编译。运行 `pnpm typecheck` 会报多处类型错误（预期，后续 Task 修复）。

- [ ] **Step 3: Commit**

```bash
git add src/core/plugin-types.ts
git commit -m "refactor(new-assembox-editor): 重定义插件契约为 definePlugin/EditorPluginObject"
```

---

## Task 3: PluginManager 实例级 register + activate 基础（去重/scene/setup/dispose）

**Files:**
- Rewrite: `src/core/plugin-manager.ts`
- Create: `src/core/__tests__/plugin-manager.test.ts`

- [ ] **Step 1: 写失败测试（实例级 register + getPlugin + options）**

新建 `src/core/__tests__/plugin-manager.test.ts`：

```ts
import {describe, it, expect, vi} from 'vitest';
import {definePlugin} from '../plugin-types';
import {PluginManager} from '../plugin-manager';
import type {EventBus} from '../event-bus';

// EventBus 极简 stub
const fakeBus = {
  on: vi.fn(() => () => {}),
  trigger: vi.fn(() => ({prevented: false, stopped: false}))
} as unknown as EventBus;

describe('PluginManager 实例级注册', () => {
  it('register 后 getPlugin 返回插件与 options', () => {
    const pm = new PluginManager(fakeBus);
    const p = definePlugin({id: 'a'});
    pm.register(p, {x: 1});
    expect(pm.getPlugin('a')?.plugin).toBe(p);
    expect(pm.getPlugin('a')?.options).toEqual({x: 1});
  });

  it('unregister 后 getPlugin 返回 undefined', () => {
    const pm = new PluginManager(fakeBus);
    pm.register(definePlugin({id: 'a'}));
    pm.unregister('a');
    expect(pm.getPlugin('a')).toBeUndefined();
  });
});
```

- [ ] **Step 2: 运行测试验证失败**

Run: `pnpm test:unit`
Expected: FAIL（PluginManager 构造签名不匹配 / register 不存在）。

- [ ] **Step 3: 重写 src/core/plugin-manager.ts（基础部分：register/getPlugin/unregister/activate/setup-dispose）**

```ts
/**
 * 插件管理器（实例级）
 * - register(plugin, options) 实例级注册（替代模块级全局表）
 * - activate: 去重(priority) → scene 过滤 → dep 拓扑排序 → setup(收集 dispose) → contributes 自动应用 → 绑钩子
 * - setup 返回的 dispose 在 destroy 统一回收
 */
import type {EventBus, EditorEvent} from './event-bus';
import {camelize} from './event-bus';
import type {EditorPluginObject, PluginContext} from './plugin-types';

export class PluginManager {
  private registered = new Map<string, {plugin: EditorPluginObject; options?: any}>();
  private instances: EditorPluginObject[] = [];
  private disposes: Array<() => void | Promise<void>> = [];
  private ctx: PluginContext | null = null;
  private unsubscribers: Array<() => void> = [];

  constructor(private readonly bus: EventBus) {}

  /** 实例级注册（携带 options） */
  register<T>(plugin: EditorPluginObject<T>, options?: T): void {
    this.registered.set(plugin.id, {plugin, options});
  }

  /** 取消注册（仅 activate 前有效） */
  unregister(id: string): void {
    this.registered.delete(id);
  }

  /** 按 id 查找（供 setup 内跨插件协作） */
  getPlugin<T = any>(id: string): {plugin: EditorPluginObject<T>; options?: T} | undefined {
    const entry = this.registered.get(id);
    return entry as {plugin: EditorPluginObject<T>; options?: T} | undefined;
  }

  /** 激活 */
  async activate(ctx: PluginContext, scene = 'desktop'): Promise<void> {
    this.ctx = ctx;

    // 1. 收集 + 2. 去重(同 id 已在 register Map 唯一，priority 不再需要去重)
    //    （同一 id 只会 register 一次，后注册覆盖）
    let plugins = Array.from(this.registered.values());

    // 3. scene 过滤
    plugins = plugins.filter(({plugin}) => {
      const sc = plugin.scene;
      if (!sc) return true;
      const arr = Array.isArray(sc) ? sc : [sc];
      return arr.includes('global') || arr.includes(scene);
    });

    // 4. dep 拓扑排序（Task 4 完善；此处先用 priority 排序）
    this.instances = this.topoSort(plugins.map(p => p.plugin));

    // 5. 依次 setup（按 this.instances 拓扑排序顺序），收集返回的 dispose
    for (const plugin of this.instances) {
      const options = this.registered.get(plugin.id)?.options;
      try {
        const ret = plugin.setup?.(ctx, options);
        const resolved = await Promise.resolve(ret);
        if (typeof resolved === 'function') {
          this.disposes.push(resolved);
        }
      } catch (err) {
        console.error(`[PluginManager] 插件 "${plugin.id}" setup 失败:`, err);
      }
    }

    // 6. contributes 自动应用（Task 6 完善）
    this.applyContributes(ctx);

    // 7. 绑钩子
    this.bindEventHooks();
  }

  /** dep 拓扑排序（Task 4 实现，此处 priority 兜底） */
  private topoSort(plugins: EditorPluginObject[]): EditorPluginObject[] {
    return [...plugins].sort((a, b) => (b.priority ?? 0) - (a.priority ?? 0));
  }

  /** contributes 自动应用（Task 6 实现，此处占位） */
  private applyContributes(_ctx: PluginContext): void {
    // Task 6 填充
  }

  /** EventBus 事件名 → 插件方法 camelize 映射 */
  private bindEventHooks(): void {
    if (!this.ctx) return;
    const hookTypes = [
      'before-insert', 'after-insert',
      'before-update', 'after-update',
      'before-delete', 'after-delete',
      'before-move', 'after-move'
    ];
    for (const type of hookTypes) {
      const methodName = camelize(type);
      const hasAny = this.instances.some(p => typeof (p as any)[methodName] === 'function');
      if (!hasAny) continue;
      const off = this.bus.on(type, (event: EditorEvent) => {
        for (const plugin of this.instances) {
          const fn = (plugin as any)[methodName];
          if (typeof fn !== 'function') continue;
          try {
            const ret = fn.call(plugin, event.context);
            if (ret === false) {
              event.preventDefault();
              event.stopPropagation();
            } else if (ret && typeof ret.then === 'function') {
              if (!event.pending) event.pending = [];
              event.pending.push(ret);
            }
          } catch (err) {
            console.error(`[PluginManager] 插件 "${plugin.id}.${methodName}" 出错:`, err);
          }
          if (event.stopped) break;
        }
      });
      this.unsubscribers.push(off);
    }
  }

  /** 广播收集 */
  buildPanels(node: any, panels: any[]): void {
    this.instances.forEach(p => {
      try { p.buildPanels?.(node, panels); }
      catch (err) { console.error(`[PluginManager] 插件 "${p.id}" buildPanels 出错:`, err); }
    });
  }
  buildToolbars(node: any, toolbars: any[]): void {
    this.instances.forEach(p => {
      try { p.buildToolbars?.(node, toolbars); }
      catch (err) { console.error(`[PluginManager] 插件 "${p.id}" buildToolbars 出错:`, err); }
    });
  }
  buildContextMenu(node: any, menus: any[]): void {
    this.instances.forEach(p => {
      try { p.buildContextMenu?.(node, menus); }
      catch (err) { console.error(`[PluginManager] 插件 "${p.id}" buildContextMenu 出错:`, err); }
    });
  }

  getPlugins(): EditorPluginObject[] {
    return this.instances;
  }

  destroy(): void {
    // 1. 逆序 dispose
    for (let i = this.disposes.length - 1; i >= 0; i--) {
      try { this.disposes[i](); }
      catch (err) { console.error('[PluginManager] dispose 出错:', err); }
    }
    // 2. 逆序调插件 destroy（EditorPluginObject 无 destroy，setup dispose 已覆盖）
    // 3. 解绑事件
    this.unsubscribers.forEach(off => off());
    this.disposes = [];
    this.unsubscribers = [];
    this.instances = [];
    this.ctx = null;
  }
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `pnpm test:unit`
Expected: PASS（2 个实例级注册测试）。

- [ ] **Step 5: Commit**

```bash
git add src/core/plugin-manager.ts src/core/__tests__/plugin-manager.test.ts
git commit -m "refactor(new-assembox-editor): PluginManager 实例级 register/activate/setup-dispose"
```

---

## Task 4: dep 拓扑排序

**Files:**
- Modify: `src/core/plugin-manager.ts`（topoSort 实现）
- Modify: `src/core/__tests__/plugin-manager.test.ts`（加测试）

- [ ] **Step 1: 写失败测试（dep 排序）**

在测试文件追加：

```ts
import type {PluginContext} from '../plugin-types';
const fakeCtx = {} as PluginContext;

describe('PluginManager dep 拓扑排序', () => {
  it('被依赖插件先 activate', async () => {
    const order: string[] = [];
    const a = definePlugin({id: 'a', dep: ['b'], setup: () => {order.push('a');}});
    const b = definePlugin({id: 'b', setup: () => {order.push('b');}});
    const pm = new PluginManager(fakeBus);
    pm.register(a); pm.register(b);
    await pm.activate(fakeCtx, 'desktop');
    expect(order).toEqual(['b', 'a']);
  });

  it('循环依赖降级 priority 不卡死', async () => {
    const a = definePlugin({id: 'a', dep: ['c'], priority: 1});
    const c = definePlugin({id: 'c', dep: ['a'], priority: 0});
    const pm = new PluginManager(fakeBus);
    pm.register(a); pm.register(c);
    await expect(pm.activate(fakeCtx, 'desktop')).resolves.toBeUndefined();
    expect(pm.getPlugins().map(p => p.id)).toContain('a');
    expect(pm.getPlugins().map(p => p.id)).toContain('c');
  });
});
```

- [ ] **Step 2: 运行测试验证失败**

Run: `pnpm test:unit`
Expected: FAIL（当前 topoSort 仅按 priority，dep 测试失败）。

- [ ] **Step 3: 实现 topoSort（Kahn 算法）**

替换 `src/core/plugin-manager.ts` 的 `topoSort` 方法：

```ts
/** dep 拓扑排序（Kahn 算法；循环依赖降级 priority 兜底） */
private topoSort(plugins: EditorPluginObject[]): EditorPluginObject[] {
  const idSet = new Set(plugins.map(p => p.id));
  const inDeg = new Map<string, number>();
  const dependents = new Map<string, string[]>(); // id -> 依赖它的 ids
  for (const p of plugins) {
    inDeg.set(p.id, 0);
    dependents.set(p.id, []);
  }
  for (const p of plugins) {
    for (const dep of p.dep ?? []) {
      if (idSet.has(dep)) {
        inDeg.set(p.id, (inDeg.get(p.id) ?? 0) + 1);
        dependents.get(dep)!.push(p.id);
      }
    }
  }
  const byPriority = (a: EditorPluginObject, b: EditorPluginObject) =>
    (b.priority ?? 0) - (a.priority ?? 0);
  const queue = plugins.filter(p => (inDeg.get(p.id) ?? 0) === 0).sort(byPriority);
  const result: EditorPluginObject[] = [];
  const visited = new Set<string>();
  const idMap = new Map(plugins.map(p => [p.id, p]));
  while (queue.length) {
    const cur = queue.shift()!;
    result.push(cur);
    visited.add(cur.id);
    for (const next of dependents.get(cur.id) ?? []) {
      inDeg.set(next, (inDeg.get(next) ?? 0) - 1);
      if (inDeg.get(next) === 0 && !visited.has(next)) {
        queue.push(idMap.get(next)!);
        queue.sort(byPriority);
      }
    }
  }
  if (result.length < plugins.length) {
    console.warn('[PluginManager] 检测到循环依赖，降级 priority 排序');
    const rest = plugins.filter(p => !visited.has(p.id)).sort(byPriority);
    return [...result, ...rest];
  }
  return result;
}
```

- [ ] **Step 4: 无需修改 setup 循环**

Task 3 已将 activate 第 5 步 setup 按 `this.instances` 顺序实现，Task 4 仅替换 `topoSort` 方法即可让 instances 顺序变为 dep 拓扑序。跳过此步。

- [ ] **Step 5: 运行测试验证通过**

Run: `pnpm test:unit`
Expected: PASS（dep 排序 + 循环降级测试）。

- [ ] **Step 6: Commit**

```bash
git add src/core/plugin-manager.ts src/core/__tests__/plugin-manager.test.ts
git commit -m "feat(new-assembox-editor): PluginManager dep 拓扑排序（Kahn + 循环降级）"
```

---

## Task 5: setup dispose 统一回收

**Files:**
- Modify: `src/core/__tests__/plugin-manager.test.ts`

- [ ] **Step 1: 写失败测试（dispose 回收 + 逆序）**

追加：

```ts
describe('PluginManager setup dispose 回收', () => {
  it('setup 返回的 dispose 在 destroy 时逆序调用', async () => {
    const calls: string[] = [];
    const a = definePlugin({id: 'a', setup: () => () => {calls.push('dispose-a');}});
    const b = definePlugin({id: 'b', setup: () => () => {calls.push('dispose-b');}});
    const pm = new PluginManager(fakeBus);
    pm.register(a); pm.register(b);
    await pm.activate(fakeCtx, 'desktop');
    pm.destroy();
    expect(calls).toEqual(['dispose-b', 'dispose-a']); // 逆序
  });

  it('单个 setup 抛错不阻断其它插件', async () => {
    const ok = vi.fn();
    const bad = definePlugin({id: 'bad', setup: () => {throw new Error('boom');}});
    const good = definePlugin({id: 'good', setup: ok});
    const pm = new PluginManager(fakeBus);
    pm.register(bad); pm.register(good);
    await pm.activate(fakeCtx, 'desktop');
    expect(ok).toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: 运行测试验证通过（Task 3 destroy 已实现逆序 dispose）**

Run: `pnpm test:unit`
Expected: PASS（若失败，检查 destroy 是否逆序 `for i = length-1 → 0`）。

- [ ] **Step 3: Commit**

```bash
git add src/core/__tests__/plugin-manager.test.ts
git commit -m "test(new-assembox-editor): 补充 setup dispose 回收与异常隔离测试"
```

---

## Task 6: contributes 自动应用

**Files:**
- Modify: `src/core/plugin-manager.ts`（applyContributes 实现）
- Modify: `src/core/__tests__/plugin-manager.test.ts`

- [ ] **Step 1: 写失败测试（contributes 自动注册到 registry）**

追加：

```ts
describe('PluginManager contributes 自动应用', () => {
  it('contributes.components 注册到 componentRegistry', async () => {
    const register = vi.fn();
    const ctx = {
      componentRegistry: {register},
      setterRegistry: {register: vi.fn()},
      actionRegistry: {register: vi.fn()},
      assetRegistry: {register: vi.fn()},
      skeleton: {add: vi.fn()}
    } as unknown as PluginContext;
    const meta = {type: 'Button'} as any;
    const p = definePlugin({id: 'p', contributes: {components: [meta]}});
    const pm = new PluginManager(fakeBus);
    pm.register(p);
    await pm.activate(ctx, 'desktop');
    expect(register).toHaveBeenCalledWith(meta);
  });

  it('contributes.skeleton 注入 editor 到 contentProps 后 skeleton.add', async () => {
    const add = vi.fn();
    const ctx = {
      componentRegistry: {register: vi.fn()},
      setterRegistry: {register: vi.fn()},
      actionRegistry: {register: vi.fn()},
      assetRegistry: {register: vi.fn()},
      skeleton: {add},
      editor: {id: 'editor-x'} as any
    } as unknown as PluginContext;
    const p = definePlugin({id: 'p', contributes: {skeleton: [{area: 'leftArea', type: 'PanelDock', name: 'd', contentProps: {k: 1}}]}});
    const pm = new PluginManager(fakeBus);
    pm.register(p);
    await pm.activate(ctx, 'desktop');
    expect(add).toHaveBeenCalledWith(expect.objectContaining({
      name: 'd',
      contentProps: expect.objectContaining({k: 1, editor: (ctx as any).editor})
    }));
  });
});
```

- [ ] **Step 2: 运行测试验证失败**

Run: `pnpm test:unit`
Expected: FAIL（applyContributes 当前占位空实现）。

- [ ] **Step 3: 实现 applyContributes**

替换 `src/core/plugin-manager.ts` 的 `applyContributes`：

```ts
/** contributes 自动应用：注册到对应 registry/skeleton */
private applyContributes(ctx: PluginContext): void {
  for (const plugin of this.instances) {
    const c = plugin.contributes;
    if (!c) continue;
    try {
      c.components?.forEach(m => ctx.componentRegistry.register(m));
      c.setters?.forEach(s => ctx.setterRegistry.register(s.name, s.component));
      c.actions?.forEach(a => ctx.actionRegistry.register(a));
      c.assets?.forEach(a => ctx.assetRegistry.register(a));
      c.skeleton?.forEach(s =>
        ctx.skeleton.add({
          ...s,
          contentProps: {...(s.contentProps ?? {}), editor: ctx.editor}
        } as any)
      );
    } catch (err) {
      console.error(`[PluginManager] 插件 "${plugin.id}" contributes 注册出错:`, err);
    }
  }
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `pnpm test:unit`
Expected: PASS（全部 contributes 测试）。

- [ ] **Step 5: Commit**

```bash
git add src/core/plugin-manager.ts src/core/__tests__/plugin-manager.test.ts
git commit -m "feat(new-assembox-editor): PluginManager contributes 自动应用（替代 editor.start 手动注册）"
```

---

## Task 7: editor 集成（ctx 含 skeleton/getPlugin、start 收敛、createEditor plugins/disableBuiltin）

**Files:**
- Modify: `src/core/editor.ts`

- [ ] **Step 1: 修改 EditorOptions 接口（加 plugins/disableBuiltin）**

`src/core/editor.ts` 顶部 `EditorOptions` 接口改为（保留 schema/platform/canvasMode，替换 plugins）：

```ts
export interface EditorOptions {
  schema?: PageSchema;
  platform?: 'desktop' | 'mobile';
  canvasMode?: 'inline' | 'iframe';
  /** 用户插件（可带 options：[plugin, options]） */
  plugins?: Array<EditorPluginObject | [EditorPluginObject, any]>;
  /** 是否禁用内置插件（默认 false） */
  disableBuiltin?: boolean;
}
```

需在文件顶部 import：

```ts
import type {EditorPluginObject} from './plugin-types';
import {builtinPlugins} from '../plugins/builtin-plugins';
```

- [ ] **Step 2: 修改 constructor 的 pluginManager 创建（注入内置 + 用户插件）**

将 constructor 内：

```ts
this.pluginManager = new PluginManager(this.bus, options.plugins);
```

替换为：

```ts
this.pluginManager = new PluginManager(this.bus);
// 注册插件：内置（除非 disableBuiltin）+ 用户插件（含 options）
if (!options.disableBuiltin) {
  for (const p of builtinPlugins) this.pluginManager.register(p);
}
for (const item of options.plugins ?? []) {
  if (Array.isArray(item)) {
    this.pluginManager.register(item[0], item[1]);
  } else {
    this.pluginManager.register(item);
  }
}
```

- [ ] **Step 3: 修改 start() 的 ctx（加 skeleton + getPlugin）+ 移除手动 contributes 处理**

将 `start()` 内（187-221 的 ctx 构建与 contributes 循环）替换为：

```ts
async start(): Promise<void> {
  flushDecorators(this.componentRegistry);
  const ctx: PluginContext = {
    editor: this,
    store: this.store,
    di: this.di,
    bus: this.bus,
    skeleton: this.skeleton,
    componentRegistry: this.componentRegistry,
    setterRegistry: this.setterRegistry,
    assetRegistry: this.assetRegistry,
    actionRegistry: this.actionRegistry,
    getPlugin: (id: string) => this.pluginManager.getPlugin(id)
  };
  // activate 内聚 contributes 自动应用 + setup + 钩子绑定
  await this.pluginManager.activate(ctx, this.store.state.platform);
}
```

（删除原 204-221 的 contributes 手动循环）

- [ ] **Step 4: 运行 typecheck**

Run: `pnpm typecheck`
Expected: 仍可能有 builtin-plugins.tsx/register.ts 的旧引用错误（Task 9 修复）；editor.ts 自身应无错误。

- [ ] **Step 5: Commit**

```bash
git add src/core/editor.ts
git commit -m "refactor(new-assembox-editor): editor.start 收敛 contributes 处理到 PluginManager，ctx 加 skeleton/getPlugin"
```

---

## Task 8: 内置插件迁移（definePlugin + coreRegistry contributes）

**Files:**
- Modify: `src/plugins/builtin-plugins.tsx`
- Modify: `src/plugins/register.ts`

- [ ] **Step 1: builtin-plugins.tsx 全部对象用 definePlugin 包装**

将每个 `export const xxxPlugin: EditorPlugin = {...}` 改为 `export const xxxPlugin = definePlugin({...})`，并在文件顶部加：

```ts
import {definePlugin} from '../core/plugin-types';
```

移除 `import type {EditorPlugin}`（若有）。`builtinPlugins` 数组类型改为 `EditorPluginObject[]`（或去掉显式类型，靠推断）。

- [ ] **Step 2: coreRegistryPlugin 的 init 注册改为 contributes**

将 `coreRegistryPlugin` 的 `init(ctx){registerBuiltinSetters(ctx.setterRegistry); registerBuiltinActions(ctx.actionRegistry);}` 改为：

```ts
export const coreRegistryPlugin = definePlugin({
  id: 'builtin-core-registry',
  name: '核心注册',
  scene: 'global',
  priority: 200,
  // 注：registerBuiltinSetters/Actions 返回 meta 列表，转 contributes 声明
  // 若二者返回的是已注册副作用而非 meta，则保留 setup：
  setup(ctx) {
    registerBuiltinSetters(ctx.setterRegistry);
    registerBuiltinActions(ctx.actionRegistry);
  }
});
```

> 说明：`registerBuiltinSetters(ctx.setterRegistry)` 是直接注册到 registry 的命令式调用（非返回 meta），因此保留在 `setup`（动态副作用）。这与 spec 一致：静态可序列化走 contributes，命令式注册走 setup。coreRegistry 的 setter/action 注册是命令式的，归 setup。

- [ ] **Step 3: 改造 register.ts（移除全局自注册，改为纯导出）**

将 `src/plugins/register.ts` 内容替换为（仅保留 builtinPlugins 聚合导出，移除 registerPlugin 全局副作用）：

```ts
/**
 * 内置插件聚合导出
 * 由 createEditor 的 options.disableBuiltin 控制（默认注入），不再模块级全局注册
 */
export {builtinPlugins} from './builtin-plugins';
```

> 若有其他模块 import `registerBuiltinPlugins`，需全局搜索替换为 `import {builtinPlugins}` + 通过 createEditor 注入。

- [ ] **Step 4: 全局搜索 registerBuiltinPlugins / registerPlugin 引用并清理**

Run: `Select-String -Path "src\**\*.ts","src\**\*.tsx" -Pattern "registerBuiltinPlugins|registerPlugin|listRegisteredPlugins|globalRegistry|from '../plugins/register'"`

逐处处理：demo/main.tsx 等若调 `registerBuiltinPlugins()`，改为 `createEditor({schema, plugins, disableBuiltin:false})`（默认即注入，可移除显式调用）。

- [ ] **Step 5: 运行 typecheck**

Run: `pnpm typecheck`
Expected: PASS（无类型错误）。

- [ ] **Step 6: Commit**

```bash
git add src/plugins/builtin-plugins.tsx src/plugins/register.ts src/demo
git commit -m "refactor(new-assembox-editor): 内置插件迁移 definePlugin，移除全局自注册"
```

---

## Task 9: 清理旧 API + index.ts 导出

**Files:**
- Modify: `src/index.ts`
- Modify: `src/core/plugin-manager.ts`（确认已无模块级 globalRegistry）

- [ ] **Step 1: index.ts 导出 definePlugin/EditorPluginObject/PluginContext**

在 `src/index.ts` 的插件导出区（原 export EditorPlugin 处）替换/新增：

```ts
export {definePlugin} from './core/plugin-types';
export type {EditorPluginObject, PluginContext, PluginContributes} from './core/plugin-types';
export {PluginManager} from './core/plugin-manager';
```

移除旧的 `export type {EditorPlugin}` 等。

- [ ] **Step 2: 确认 plugin-manager.ts 已无模块级 globalRegistry/registerPlugin**

Run: `Select-String -Path "src\core\plugin-manager.ts" -Pattern "globalRegistry|export function registerPlugin|listRegisteredPlugins"`
Expected: 无匹配（Task 3 重写已删除）。

- [ ] **Step 3: 运行 typecheck**

Run: `pnpm typecheck`
Expected: PASS。

- [ ] **Step 4: Commit**

```bash
git add src/index.ts src/core/plugin-manager.ts
git commit -m "refactor(new-assembox-editor): 导出 definePlugin 契约，清理旧全局注册 API"
```

---

## Task 10: 全量回归（单测 + e2e）+ 收尾

- [ ] **Step 1: 运行全部单测**

Run: `pnpm test:unit`
Expected: PASS（所有 plugin-manager 测试通过）。

- [ ] **Step 2: 运行 typecheck**

Run: `pnpm typecheck`
Expected: PASS。

- [ ] **Step 3: 运行 e2e 回归**

Run: `pnpm test:e2e`
Expected: `2 passed`（画布/组件面板/设置面板正常渲染，验证迁移未破坏）。

- [ ] **Step 4: 若 e2e 失败，按报错定位迁移遗漏（通常为骨架面板 contentProps/skeleton.add）并修复**

- [ ] **Step 5: 最终提交（若有修复）**

```bash
git add -A
git commit -m "test(new-assembox-editor): 插件体系重构全量回归通过"
```

---

## 自审检查清单（实现完成后）

- [ ] spec 第三章契约（EditorPluginObject/definePlugin/三入口）→ Task 2 ✓
- [ ] spec 第四章 PluginManager（实例级/dep/dispose/contributes自动）→ Task 3-6 ✓
- [ ] spec 第五章 PluginContext（skeleton/getPlugin）→ Task 2/7 ✓
- [ ] spec 第六章迁移（7 插件/register.ts/editor.start）→ Task 7-9 ✓
- [ ] spec 第七章错误处理（全 try-catch）→ Task 3 ✓
- [ ] spec 第八章测试（vitest 单测 + e2e 回归）→ Task 1/3-6/10 ✓
- [ ] spec 第十节非目标（不做动态加载/preference 校验）→ 未越界 ✓
