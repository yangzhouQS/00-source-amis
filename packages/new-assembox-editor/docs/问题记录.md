

## 2026-8-5 16:26:02
- 修改后的JSON格式没有再深入处理过，编辑器是参考之前amis的json结构开始开发的
- 组件选择后右上角的删除、复制、上移和下移，需要重新按照新的json结构从新调试
  - 希望在数组插槽内才支持复制和上移下移
  - 其他的组件只需要支持删除即可
  - 组件选择后，辅助区域默认操作：组件名称（组件至根节点组件链路）、父组件、复制、删除
- 画布中的组件拖拽移动位置支持（难度大）
- 组件拖至画布优化
  - 拖动至画布的ghost样式
  - 拖拽放置位置的检测、插槽检测
- 画布渲染插槽识别处理问题（最难处理）
  - 现在是在渲染器外围包裹了一层div，导致布局和样式受到影响，flex-line 左右插槽会自动根据元素el-space会处理，包裹后处理错误
  - box插槽高度现在没有撑开
  - 建议在渲染库打包ast中进行侵入式改造
- 组件属性配置
  - 组件属性描述格式确定
  - setter配置每个都需要测试
  - 属性配置面板设计
  - 配置属性分组支持
  - 组件每个属性之间的相互卡控，互斥操作（旧版不支持，不紧急的功能）
  - 样式属性配置面板设计，借鉴lowcode和旧版的配置实现，基本满足使用
  - 事件配置面板设计
  - 共享函数、数据配置面板（暂时没有）


